name: ci
on:
  push:
    branches: [main]
    tags: ['v*']
  pull_request:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Validate
        run: |
          jq -e .KPlugin.Id metadata.json
          bash -n contents/scripts/claude-usage

      - name: Test
        run: bash tests/test-claude-usage.sh

      - name: Test labels.js
        run: node tests/test-labels.js

      - name: Test refresh floor
        run: node tests/test-refresh-floor.js

      - name: Test stale-data handling
        run: node tests/test-stale-data.js

      - name: Build .plasmoid
        run: bash pack.sh

      - uses: actions/upload-artifact@v4
        with:
          name: plasmoid
          path: '*.plasmoid'

      - name: Release
        if: startsWith(github.ref, 'refs/tags/v')
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh release create "$GITHUB_REF_NAME" *.plasmoid --title "$GITHUB_REF_NAME" --generate-notes
